<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.vladik.dnevnik">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:allowBackup="false">
        <activity
            android:name="ru.vladik.dnevnik.Activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ru.vladik.dnevnik.Activities.MainActivity"
            android:exported="false">
        </activity>

        <activity android:name="ru.vladik.dnevnik.Activities.MarkInfoActivity"
            android:exported="false">
        </activity>

        <activity android:name="ru.vladik.dnevnik.Activities.RatingActivity"
            android:exported="false">
        </activity>
    </application>

</manifest>